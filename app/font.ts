import { Geist, Geist_Mono, Inter, Manrope, Noto_Sans } from "next/font/google";

export const geistSans = Geist({
  subsets: ["latin"],
  display: 'swap'
});

export const geistMono = Geist_Mono({
  subsets: ["latin"],
  display: 'swap'
});

export const manrope = Manrope({
  subsets: ["latin"],
  display: 'swap'
});

export const inter = Inter({
  subsets: ["latin"],
  display: 'swap'
});

export const notoSans = Noto_Sans({
  subsets: ["latin"],
  display: 'swap'
});